services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_2GIS_API_KEY: ${NEXT_PUBLIC_2GIS_API_KEY}
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_2GIS_API_KEY: ${NEXT_PUBLIC_2GIS_API_KEY}
    ports:
      - "127.0.0.1:9094:3000"
